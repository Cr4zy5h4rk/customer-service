apiVersion: v1
kind: Secret
metadata:
  name: catalog-postgresql
  namespace: default
  labels:
    app: catalog-postgresql
type: Opaque
data:
  postgresql-password: NWI5ZjUzZjY1MDYxMWJkZmI2MDFmNDhlNGU1MmI1ZDJmOTViOTZkMjIxNWFmMjdlMzYzYTdkN2YxYTlj
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog-postgresql
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalog-postgresql
  template:
    metadata:
      labels:
        app: catalog-postgresql
    spec:
      volumes:
        - name: data
          emptyDir: {}
      containers:
        - name: postgres
          image: postgres:18.6
          env:
            - name: POSTGRES_USER
              value: catalog
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: catalog-postgresql
                  key: postgresql-password
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
              subPath: postgres
          resources:
            requests:
              memory: '512Mi'
              cpu: '500m'
            limits:
              memory: '1Gi'
              cpu: '1'
---
apiVersion: v1
kind: Service
metadata:
  name: catalog-postgresql
  namespace: default
spec:
  selector:
    app: catalog-postgresql
  ports:
    - port: 5432
